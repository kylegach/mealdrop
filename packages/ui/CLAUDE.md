When looking in this package:

- Never look at the source code of components.
- Always use the `list-all-documentation` MCP tool to look up component info. 
- After looking up the list of components, use the `get-documentation` tool to get more info on a specific component, and the `get-documentation-for-story` tool to get info on a specific story.
